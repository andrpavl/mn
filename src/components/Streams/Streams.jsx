import styles from "./Streams.module.css";

export default function Streams() {
    return (
        <div className={styles.streamsContainer}>
          <h2>Наступні стріми:</h2>
        </div>
    );
}